using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroySpawner200 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 200f);
	}

	// Update is called once per frame
	void Update () {

	}
}
