using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroySpawner75 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 75f);
	}

	// Update is called once per frame
	void Update () {

	}
}
