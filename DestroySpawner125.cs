using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroySpawner125 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 125f);
	}

	// Update is called once per frame
	void Update () {

	}
}
