using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroySpawner100 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 100f);
	}

	// Update is called once per frame
	void Update () {

	}
}
