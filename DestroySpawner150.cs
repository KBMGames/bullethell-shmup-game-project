using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroySpawner150 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Destroy (gameObject, 150f);
	}

	// Update is called once per frame
	void Update () {

	}
}
